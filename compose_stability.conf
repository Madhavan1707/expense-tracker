// java.time values are immutable, but they live outside this module so the
// Compose compiler cannot prove it and treats them as unstable. That made
// MonthHeatmap and all 42 DayCells recompose on every frame.
java.time.LocalDate
java.time.YearMonth
java.time.LocalDateTime
java.time.Instant
